name: Amazon exact pipeline rotation validation

on:
  workflow_dispatch:
    inputs:
      rate:
        description: 'Exact-pipeline request starts per second'
        required: true
        default: '1.6'
      rotate_every:
        description: 'Primary reads per browser-like session'
        required: true
        default: '8'

permissions:
  contents: read

concurrency:
  group: amazon-exact-pipeline-rotation-validation
  cancel-in-progress: false

jobs:
  validate-read-only:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment: runtime
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Prepare isolated validation
        shell: bash
        env:
          ARCHIVE_KEY: ${{ secrets.ARCHIVE_KEY }}
        run: |
          set -Eeuo pipefail
          umask 077
          work="$RUNNER_TEMP/amazon-exact-pipeline-rotation"
          mkdir -p "$work"
          printf '%s' "$ARCHIVE_KEY" | gpg --quiet --batch --yes --pinentry-mode loopback \
            --passphrase-fd 0 --decrypt --output "$work/payload.tgz" .assets/node.dat
          tar -xzf "$work/payload.tgz" -C "$work"
          test -f "$work/scanner.py"
          cp diagnostics/amazon_exact_pipeline_rotation_validation.py "$work/"
          python -m pip install --disable-pip-version-check --no-cache-dir -r "$work/requirements.txt"

      - name: Validate exact pipeline with bounded session rotation
        shell: bash
        env:
          AMAZON_DIAGNOSTIC_RATE: ${{ inputs.rate }}
          AMAZON_DIAGNOSTIC_SESSION_ROTATE: ${{ inputs.rotate_every }}
        run: |
          set -Eeuo pipefail
          cd "$RUNNER_TEMP/amazon-exact-pipeline-rotation"
          python amazon_exact_pipeline_rotation_validation.py

      - name: Preserve validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: amazon-exact-pipeline-rotation-validation-results
          path: ${{ runner.temp }}/amazon-exact-pipeline-rotation/audit_results/amazon_exact_pipeline_rotation_*/results.json
          if-no-files-found: warn
          retention-days: 14

      - name: Cleanup
        if: always()
        shell: bash
        run: rm -rf "$RUNNER_TEMP/amazon-exact-pipeline-rotation"
